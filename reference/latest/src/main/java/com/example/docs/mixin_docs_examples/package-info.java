/**
 * Package to hold sanitized, controlled examples for the Mixin docs for the sake of a clean demonstration.
 * 
 * The classes shown here may be incomplete, or only make sense in the context of the Docs surrounding them, and may at times
 * be broken into "stages" depending on the needs.
 * 
 * For a class broken into "stages", the class will be in its own folder and the stages will be divided into `Name_StageNumber`, for example
 * `MyMixin_0`, `MyMixin_1`, etc. would all be different stages or forms of the same mixin class, split for the sake of documentation because the
 * regioning format for snippets was not enough.
 */
package com.example.docs.mixin_docs_examples;
