def minecraftVersion = "1.21.5"
def yarnVersion = "1.21.5+build.1"
def fabricApiVersion = "0.119.5+1.21.5"

dependencies {
  minecraft "com.mojang:minecraft:${minecraftVersion}"
  mappings "net.fabricmc:yarn:${yarnVersion}:v2"
  modImplementation "net.fabricmc.fabric-api:fabric-api:${fabricApiVersion}"
}

// :::automatic-testing:2
test {
	useJUnitPlatform()
}
// :::automatic-testing:2
