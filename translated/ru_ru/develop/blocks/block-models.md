---
title: Модели блоков (Block Models)
description: "**Описание:** руководство по созданию и пониманию моделей блоков."
authors:
  - Fellteros
  - its-miroma
---

<!-- markdownlint-disable search-replace -->

Эта страница поможет тебе написать собственные модели блоков и понять все их параметры и возможности.

## Что такое модели блоков? {#what-are-block-models}

Модели блоков определяют внешний вид блока — его визуальную составляющую. Они задают текстуру, позицию модели, вращение, масштаб и другие параметры.

Модели хранятся в виде файлов **JSON** в папке `resources`.

## Структура файла {#file-structure}

Каждый файл модели блока имеет определённую структуру. Он начинается с пустых фигурных скобок `{}`, которые обозначают **корневой тег** модели. Вот краткая схема структуры блочных моделей:

```json
{
  "parent": "...",
  "ambientocclusion": "true/false",
  "display": {
    "<position>": {
      "rotation": [0.0, 0.0, 0.0],
      "translation": [0.0, 0.0, 0.0],
      "scale": [0.0, 0.0, 0.0]
    }
  },
  "textures": {
    "particle": "...",
    "<texture_variable>": "..."
  },
  "elements": [
    {
      "from": [0.0, 0.0, 0.0],
      "to": [0.0, 0.0, 0.0],
      "rotation": {
        "origin": [0.0, 0.0, 0.0],
        "axis": "...",
        "angle": "...",
        "rescale": "true/false"
      },
      "shade": "true/false",
      "light_emission": "...",
      "faces": {
        "<key>": {
          "uv": [0, 0, 0, 0],
          "texture": "...",
          "cullface": "...",
          "rotation": "...",
          "tintindex": "..."
        }
      }
    }
  ]
}
```

<!--@include: ../items/item-models.md#parent-->

Установи этот тег в значение `builtin/generated`, чтобы использовать модель, созданную на основе указанного изображения (иконки). Вращение можно задавать через [blockstates](./blockstates).

### Ambient Occlusion (затемнение) {#ambient-occlusion}

```json
{
  "ambientocclusion": "true/false"
}
```

Этот тег указывает, нужно ли использовать **ambient occlusion** (окружающее затемнение). По умолчанию — `true`.

<!--@include: ../items/item-models.md#display-->

### Текстуры {#textures}

```json
{
  "textures": {
    "particle": "...",
    "<texture_variable>": "..."
  }
}
```

Тег `textures` содержит текстуры модели в виде идентификаторов или переменных текстур. Он включает три объекта:

1. `particle`: _String_. `particle`: **строка** — определяет текстуру, из которой берутся частицы. Эта же текстура используется как наложение в портале в ад, а также для статичных текстур воды и лавы. Также она является текстурной переменной, к которой можно обратиться как `#particle`.
2. `<texture_variable>`: **строка** — создаёт переменную и назначает ей текстуру. Создает переменную и присваивает ей текстуру. К ней потом можно обратиться через `#` (например, `"top": "namespace:path"` → `#top`).

<!--@include: ../items/item-models.md#elements-->

<!--@include: ../items/item-models.md#from-->

`from` определяет начальную точку кубоида в формате `[x, y, z]`, относительно нижнего левого угла. `to` задаёт конечную точку. Обычный блок начинается в `[0, 0, 0]` и заканчивается в `[16, 16, 16]`.
Значения должны быть в диапазоне от **-16** до **32**, то есть модель может быть максимум размером **3×3 блока**.

<!--@include: ../items/item-models.md#rotation-->

`rotation` задаёт вращение элемента и включает: Он содержит еще 4 значения:

1. `origin`: _Three floats_. `origin`: **три числа с плавающей точкой** — центр вращения `[x, y, z]`.
2. `axis`: **строка** — ось вращения: `x`, `y` или `z`. Указывает направление вращения и должно быть одним из следующих: `x`, `y` и `z`.
3. `angle`: _Float_. Указывает угол поворота. `angle`: **число с плавающей точкой** — угол вращения от **-45** до **45** градусов.
4. `rescale`: _Boolean_. Указывает, следует ли масштабировать грани по всему блоку. По умолчанию `false`.

<!--@include: ../items/item-models.md#shade-to-faces-->

1. `uv`: _Four integers_. `uv`: **четыре целых числа** — область текстуры `[x1, y1, x2, y2]`. Если не указано, значения берутся из позиции элемента.
   Перестановка `x1` и `x2` (например, `0, 0, 16, 16` → `16, 0, 0, 16`) отражает текстуру. Если `uv` не указано, оно генерируется автоматически.
2. `texture`: **строка** — ссылка на текстуру в формате `#<имя_переменной>`. Указывает текстуру лица в виде [переменной текстуры](#textures), перед которой ставится символ `#`.
3. `cullface`: _String_. `cullface`: **строка** — может быть: `down`, `up`, `north`, `south`, `west`, `east`. Указывает, нужно ли рендерить грань, если в указанном положении к ней примыкает блок.
   Он также определяет сторону блока, с которой будет использоваться уровень освещенности для освещения лица, и, если не установлен, по умолчанию используется сторона.
4. `rotation`: _Integer_. `rotation`: **целое число** — поворачивает текстуру по часовой стрелке на кратное 90° значение. Вращение не влияет на то, какая часть текстуры используется.
   Вместо этого это сводится к перестановке выбранных вершин текстуры (выбранных неявно или явно через `uv`).
5. `tintidex`: _Integer_. Окрашивает текстуру на этом лице с помощью значения оттенка. Значение `-1` означает, что оттенок не используется.
   Любой другой номер передается в `BlockColors` для получения значения оттенка, соответствующего этому индексу (возвращает белый цвет, если для блока не задан индекс оттенка).

## Источники и ссылки {#sources-and-links}

Более подробное руководство можно найти на странице Minecraft Wiki:  
[Block Models (Модели блоков)](https://minecraft.wiki/w/Model#Block_models) Много информации здесь взято с этой страницы.
